<div class="max-w-3xl mx-auto mt-10 px-4">
  <h1 class="text-2xl font-bold mb-6 text-white">Detalles de la Instalación</h1>

  <div class="bg-white shadow-md rounded-lg p-6 space-y-4">
    <p><strong>Cliente:</strong> <%= @installation.customer_name %></p>
    <p><strong>Dirección:</strong> <%= @installation.address %></p>
    <p><strong>Inicio:</strong> <%= l(@installation.starts_at, format: :long) %></p>
    <p><strong>Fin:</strong> <%= l(@installation.ends_at, format: :long) %></p>
    <p><strong>Técnico asignado:</strong> <%= @installation.technician.name %></p>
  </div>

  <div class="mt-6 flex gap-4">
    <%= link_to "Editar", edit_installation_path(@installation), class: "bg-yellow-500 hover:bg-yellow-600 text-white px-4 py-2 rounded" %>
    <%= link_to "Volver al listado", dashboard_path, class: "bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded" %>
  </div>
</div>