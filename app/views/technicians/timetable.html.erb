<h1 class="text-2xl font-bold text-center mb-6 text-white">Agenda del técnico</h1>

<p class="text-center text-white">Aquí se muestra la programación diaria del técnico <strong><%= @technician.name %></strong>.</p>

<ul class="mt-6 space-y-4">
  <% @installations.each do |installation| %>
    <li class="bg-white bg-opacity-80 backdrop-blur p-4 rounded shadow">
      <strong>Cliente:</strong> <%= installation.customer_name %><br>
      <strong>Dirección:</strong> <%= installation.address %><br>
      <strong>Inicio:</strong> <%= l(installation.starts_at, format: :long) %><br>
      <strong>Fin:</strong> <%= l(installation.ends_at, format: :long) %>
    </li>
  <% end %>
</ul>

<div class="mt-6 text-center text-white">
  <%= link_to "Volver al dashboard", dashboard_path, class: "text-white hover:underline" %>
</div>